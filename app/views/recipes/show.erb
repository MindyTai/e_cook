<h1><%= @recipe.title %></h1>
<h1><%= @recipe.description %></h1>
<%= image_tag @recipe.photo.variant(resize_to_limit: [300, 300]) if @recipe.photo.attached?%>
<%
=begin%>
 <%= image_tag @recipe.photo.variant(resize: "100x100") if @recipe.photo.attached?%> 
<%
=end%>
<%= link_to '食品首頁', "/" %>