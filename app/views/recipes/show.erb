<h1><%= @recipe.title %></h1>
<h1><%= @recipe.description %></h1>
<%
=begin%>
 <%= image_tag @recipe.photo.variant(resize_to_limit: [300, 300]) if @recipe.photo.attached?%> 
<%
=end%>

 <%= image_tag @recipe.photo.variant(resize: "300x300") if @recipe.photo.attached?%> 

<%= link_to '食品首頁', "/" %>